import math

# 1行に空白区切りの数字
#a = [int(ch) for ch in input().split() ]
# N行に空白区切りの数字
#a = [ [int(ch) for ch in input().split()] for i in range(N) ]
# 素数リスト (import mathすること)
# primes = [x for x in range(2, 1000) if all([x%y != 0 for y in range(2, math.floor(math.sqrt(x)))])]